[package]
name = "rust_from_python"
version = "0.1.0"
authors = ["Arvid Boisen"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "rust_lib"
crate-type = ["dylib"]

[dependencies]
